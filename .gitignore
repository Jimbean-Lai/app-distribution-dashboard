__pycache__/
*.pyc
downloads/
.venv/
venv/
dist/
build/
*.egg-info/
.DS_Store

# 凭证/私钥/密钥（禁止入库）
**/credentials.json
**/*.credentials.json
!config/example.credentials.json
凭证/
config/credentials.json
*.cer
*.pem
*.key
*.p12
*.jks
*.rtf

# 华为 agconnect/服务账号等敏感 JSON
**/*acg*.json
**/*agc*.json
**/*service-account*.json
**/*haiwai*.json

# 本地应用目录（含真实包名/路径），GitHub 用 catalog.example.json
apps/catalog.json

# 应用图标本地缓存（分享图用，按 app_id 自动生成）
apps/icons/

# 本地构建产物（APK/AAB，体积大不入库）
builds/

# 自动生成的发布任务历史（每次重启覆盖，不入库）
config/tasks_history.json*
.venv-setup.log

# 换肤前的前端备份（本地保留，不入库）
app_store/templates/*.bak*

# 需求反馈+调研整合文档（含待填联系方式，不公开）
docs/开放平台API能力需求与调研.md

# AI 代理指引文件（本地使用，不入库）
AGENTS.md

# 本地审查文档（不入库）
docs/bug修复清单.md
docs/代码审查报告.md
docs/UI审计报告.md

# 本地临时文档（AI 生成分析/清单等，不入库）
tmpdoc/

# 看板登录账号+飞书机器人配置（本地私有，模板 example.board.json）
config/board.json
!config/example.board.json

# 登录/操作审计日志（本地私有）
config/login_log.jsonl

# 各平台官方 API 文档存档（本地参考用，不入库）
docs/*_api_guide.txt
