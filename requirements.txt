requests                  # 各平台 HTTP API 调用
requests-toolbelt>=1.0.0    # 上传进度回调（MultipartEncoderMonitor）
cryptography>=41.0          # Apple App Store Connect API JWT 签名（ES256）
pycryptodome              # 小米 X509 公钥 RSA 加密签名
androguard                # APK/AAB 元信息解析（app_store/apk_meta.py）
Pillow>=10.0              # 飞书分享图渲染（app_store/share_image.py）
lark-oapi>=1.3            # 飞书机器人官方 SDK（app_store/feishu_bot.py）
# Google Play 适配器需要：
#   pip install "google-api-python-client>=2.100" "google-auth>=2.23" "google-auth-httplib2>=0.1"
# 或用 pip install -e ".[google]"
